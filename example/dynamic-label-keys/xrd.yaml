apiVersion: apiextensions.crossplane.io/v2
kind: CompositeResourceDefinition
metadata:
  name: labeldemos.example.crossplane.io
spec:
  group: example.crossplane.io
  names:
    kind: LabelDemo
    plural: labeldemos
  scope: Namespaced
  versions:
  - name: v1alpha1
    served: true
    referenceable: true
    schema:
      openAPIV3Schema:
        type: object
        properties:
          spec:
            type: object
            properties:
              environment:
                type: string
              team:
                type: string
              serviceID:
                type: string
            required:
            - environment
            - team
            - serviceID
